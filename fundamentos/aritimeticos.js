const [a, b, c, d] = [1, 2, 3, 4]
const soma = a + b + c + d
const subtracao = d - a
const multiplicacao = b * d
const divisao = d / b
const modulo = c % b

console.log(soma, subtracao, multiplicacao, divisao, modulo)
